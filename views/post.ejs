<%- include("partials/header.ejs") %>

<section class="container mt-5">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <h1 class="fw-bold my-font-family"><%= post.title %></h1>
            <p class="text-muted">Published on <%= new Date(post.created_at).toLocaleDateString("pt-PT") %></p>
            <% if (post.image_path) { %>
                <img src="<%= post.image_path %>" alt="Post image" class="img-fluid my-4">
            <% } %>
            <p class="my-font-family fs-5" style="white-space: pre-line;"><%= post.content %></p>
                <!-- o conteúdo inserido no formulário (new-post.ejs) pode conter quebras de linha, mas 
                ao ser renderizado no post.ejs, o EJS trata essas quebras como texto simples, 
                ignorando o formato de parágrafos.
                Por padrão, o HTML não interpreta quebras de linha (\n) como novas linhas visíveis na página.
                Para resolver esse problema implementa-se style:"white-space: pre-line;"-->
            <a href="/" class="btn btn-secondary mt-3">Back to Home</a>
        </div>
    </div>
</section>

<%- include("partials/footer.ejs") %>
